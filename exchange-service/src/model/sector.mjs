'use strict'

/**
 * A model that represents a sector.
 */
export default class Sector {
    constructor(
        field,
        performance
    ) {
        this.field = field
        this.performance = performance
    }
}