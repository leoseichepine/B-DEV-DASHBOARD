'use strict'

/**
 * The model of the about response.
 */
export default class About {
    constructor(
        customer,
        server
    ) {
        this.customer = customer
        this.server = server
    }
}