'use strict'

/**
 * The model of the service.
 */
export default class Service {
    constructor(
        name,
        widgets
    ) {
        this.name = name
        this.widgets = widgets
    }
}